# Built sidecars live here (never commit binaries).
# Regenerate with: pwsh build-aux/fetch-obs.ps1 (Windows)
#                  bash build-aux/fetch-obs.sh  (Linux, also fetches obs-cmd)
#                  + build-aux/fetch-ffmpeg.ps1 / .sh for the editor FFmpeg.
# CI fetches them per release; end-user packages ship them inside.
*/
!.gitkeep
